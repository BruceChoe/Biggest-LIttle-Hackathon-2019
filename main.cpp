#pragma once
#include "Blockchain.h"

int main() {
	/*
	Blockchain bChain = Blockchain();
	Report rep1();
	Report rep2();
	Report rep3();

	cout << "Mining block 1..." << endl;
	bChain.AddBlock(Block(1, "Block 1 Data", rep1));

	cout << "Mining block 2..." << endl;
	bChain.AddBlock(Block(2, "Block 2 Data", rep2));

	cout << "Mining block 3..." << endl;
	bChain.AddBlock(Block(3, "Block 3 Data", rep3));
	*/
	return 0;
}
